<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="15dp"
    android:layout_marginEnd="15dp"
    android:layout_marginTop="8dp"
    android:layout_marginBottom="8dp"
    android:padding="10dp"
    android:background="@drawable/large_button_layout_filled_thin">

    <ImageView
        android:id="@+id/accountItemLayoutIcon"
        android:layout_width="45dp"
        android:layout_height="45dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:src="@drawable/cash_icon"/>

    <TextView
        android:id="@+id/accountItemLayoutName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:fontFamily="@font/roboto_medium"
        android:text="Account "
        android:textColor="@color/accent_color"
        android:textSize="16sp"
        android:layout_marginTop="-1dp"
        app:layout_constraintStart_toEndOf="@id/accountItemLayoutIcon"
        app:layout_constraintTop_toTopOf="@id/accountItemLayoutIcon" />

    <TextView
        android:id="@+id/accountItemLayoutBalanceText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:fontFamily="@font/roboto_medium"
        android:text="Balance: "
        android:textColor="@color/accent_color"
        android:textSize="16sp"
        android:layout_marginBottom="-1dp"
        app:layout_constraintBottom_toBottomOf="@id/accountItemLayoutIcon"
        app:layout_constraintStart_toEndOf="@id/accountItemLayoutIcon" />

    <TextView
        android:id="@+id/accountItemLayoutBalance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:fontFamily="@font/roboto_medium"
        android:text="1,000.00"
        android:textColor="@color/accent_color"
        android:textSize="16sp"
        android:layout_marginBottom="1dp"
        app:layout_constraintBottom_toBottomOf="@+id/accountItemLayoutBalanceText"
        app:layout_constraintStart_toEndOf="@+id/accountItemLayoutBalanceText"
        app:layout_constraintTop_toTopOf="@+id/accountItemLayoutBalanceText" />

    <ImageView
        android:id="@+id/accountItemLayoutOptionButton"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:clickable="true"
        android:focusable="true"
        android:src="@drawable/options_menu_icon"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginEnd="5dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:tint="@color/accent_color" />

</androidx.constraintlayout.widget.ConstraintLayout>